# Asian Kitchen Menu

A modern Asian kitchen menu with category-based filtering.

## Features
- Menu data is defined as an array of objects in JavaScript
- Category buttons are generated dynamically
- Uses `map`, `reduce`, and `filter` for rendering and filtering

## Run
Open `index.html` in a browser.
